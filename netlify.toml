[build]
  command = "npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Nota: Este projeto é uma aplicação Full Stack. 
# O Netlify serve apenas os arquivos estáticos. 
# Para o Admin funcionar, você precisará configurar as variáveis de ambiente no painel do Netlify.
